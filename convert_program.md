# 番組表データ変換プログラム仕様書 (`convert_program.md`)

## 1. 概要

本プログラムは、ボートレース公式サイトからダウンロードした番組表のテキストファイル（生データ）を解析し、機械学習などで扱いやすいCSV形式のデータに変換することを目的とする。

## 2. 機能仕様

-   指定された日付の番組表テキストファイルを読み込む。
-   テキストファイルからレースごとの情報（レース場、レース番号、距離、締切時間など）を抽出する。
-   各レースに出場する選手の情報（登録番号、年齢、支部、体重、勝率など）を抽出する。
-   抽出したデータを整形し、1選手1行の形式でCSVファイルに追記する。

## 3. 実行方法

### コマンドライン

```bash
python convert_program.py YYYY-MM-DD
```

### 引数

-   `YYYY-MM-DD`: 処理対象のデータの日付を `年-月-日` の形式で指定する。(例: `2025-07-13`)

## 4. 入力ファイル仕様

-   **パス**: `data/raw/programs/b{YY}{MM}{DD}_u8.txt`
    -   `{YY}`: 年の下2桁 (例: 2025年 -> `25`)
    -   `{MM}`: 月 (2桁、ゼロ埋め)
    -   `{DD}`: 日 (2桁、ゼロ埋め)
-   **フォーマット**: 固定長のテキストファイル
-   **文字コード**: UTF-8

## 5. 出力ファイル仕様

-   **パス**: `data/programs.csv`
-   **フォーマット**: CSV (Comma-Separated Values)
-   **文字コード**: UTF-8
-   **書き込みモード**: 追記（ファイルが存在しない場合は新規作成し、ヘッダー行を出力する）
-   **カラム定義**:

| No. | カラム名 | データ型 | 説明 |
|:--- |:---|:---|:---|
| 1 | 年 | Integer | レース開催年 (西暦) |
| 2 | 月 | Integer | レース開催月 |
| 3 | 日 | Integer | レース開催日 |
| 4 | レース場番号 | String | レース場の識別番号 (例: "01" - 桐生) |
| 5 | レース番号 | Integer | 1から12までのレース番号 |
| 6 | 距離 | Integer | レース距離 (メートル) |
| 7 | 投票締切時間 | String | 投票締切時間 (HH:MM形式) |
| 8 | 枠番 | Integer | 選手の枠番 (1-6) |
| 9 | 選手登番 | Integer | 選手の登録番号 (4桁) |
| 10 | 年齢 | Integer | 選手の年齢 |
| 11 | 支部 | String | 選手の所属支部 |
| 12 | 体重 | Float | 選手の体重 (kg) |
| 13 | 級別 | String | 選手の級別 (A1, A2, B1, B2) |
| 14 | 全国勝率 | Float | 全国での勝率 |
| 15 | 全国2連率 | Float | 全国での2連対率 |
| 16 | 当地勝率 | Float | 開催レース場での勝率 |
| 17 | 当地2連率 | Float | 開催レース場での2連対率 |
| 18 | モーター番号 | Integer | 使用するモーターの番号 |
| 19 | モーター2連率 | Float | 使用するモーターの2連対率 |
| 20 | ボート番号 | Integer | 使用するボートの番号 |
| 21 | ボート2連率 | Float | 使用するボートの2連対率 |

## 6. 処理フロー（詳細）

1.  **プログラム実行と引数解析 (`main`関数)**
    1.  プログラムはコマンドラインから `python convert_program.py YYYY-MM-DD` の形式で実行される。
    2.  引数の数が正しいか（日付が1つ指定されているか）を検証する。
    3.  引数の日付文字列が `YYYY-MM-DD` のフォーマットに一致するかを正規表現で検証する。
    4.  日付文字列を `-` で分割し、年・月・日の各要素が妥当な範囲にあるか（年: 1900-2100, 月: 1-12, 日: 1-31）をチェックする。
    5.  `datetime`モジュールを使い、指定された年月日がカレンダー上に実在する日付か（例: 2月30日でないか）を検証する。

2.  **変換処理の準備**
    1.  `data` ディレクトリが存在しない場合に備えて作成する (`os.makedirs`)。
    2.  `ProgramConverter` クラスのインスタンスを生成する。この際、クラス内部でレース場名と番号のマッピング辞書、CSVのヘッダーリストが初期化される。

3.  **ファイル変換の実行 (`convert_file`メソッド)**
    1.  引数で受け取った年月日を基に、入力ファイル名 (`b{YY}{MM}{DD}_u8.txt`) を生成する。
    2.  入力ファイルが `data/raw/programs/` に存在するかを確認し、なければエラーメッセージを表示して終了する。
    3.  入力ファイルをUTF-8エンコーディングで読み込み、全行をリストとして取得する。

4.  **データ解析ループ**
    1.  ファイル全体の行をループ処理する。
    2.  `current_track_number` 変数を `None` で初期化し、現在処理中のレース場を追跡する。
    3.  行の先頭が `\d{2}BBGN` (例: `01BBGN`) に一致する場合、その2桁の数字を `current_track_number` に設定し、レース場データの開始と判断する。
    4.  行に `ボートレース` という文字列が含まれ、かつ `current_track_number` がまだ設定されていない場合、レース場名からマッピング辞書を使ってレース場番号を特定し、`current_track_number` に設定する。
    5.  行に `Ｒ` または `R` が含まれ、かつ `電話投票締切予定` という文字列が含まれる場合、その行をレースヘッダーと判断し、`process_race_section` メソッドを呼び出してレース区間の処理を開始する。

5.  **レース区間処理 (`process_race_section`メソッド)**
    1.  レースヘッダー行からレース番号、距離、締切時間を正規表現で抽出する。
    2.  次のレースヘッダー (`...R...`) またはファイル終端マーカー (`BEND`, `FINALB`) が現れるまで、後続の行を読み進める。
    3.  各行について `parse_boat_data` メソッドを呼び出し、選手情報の解析を試みる。
    4.  `parse_boat_data` が選手情報を返した場合（`None`でない場合）、その情報を艇番をキーとする辞書に格納する。
    5.  区間の処理が終了した時点で、選手データが6人分揃っていれば、レース情報と選手情報をまとめた辞書を返す。

6.  **選手情報解析 (`parse_boat_data`メソッド)**
    1.  渡された行がヘッダーや区切り線でないことを確認する。
    2.  行の長さが十分にあるか（最低58文字）をチェックする。
    3.  固定長フォーマットに基づき、行の特定の位置からスライスして各データ（選手登番, 年齢, 支部, 体重, 勝率など）を文字列として抽出する。
    4.  抽出した選手登番が4桁の数字であるなど、データが妥当であるかを検証し、問題がなければ情報を辞書として返す。

7.  **CSVファイルへの書き込み**
    1.  解析ループが完了した後、出力ファイル `data/programs.csv` を追記モード (`'a'`) で開く。
    2.  ファイルが新規作成された場合（`os.path.exists`で判定）のみ、`csv_headers` に基づいてヘッダー行を書き込む。
    3.  解析してメモリ上に保持しておいた全レースのデータをループ処理する。
    4.  1つのレースデータに含まれる6人の選手それぞれについて、CSVの1行分のデータリストを作成する。
    5.  作成した行データを `csv.writer` を使ってファイルに書き込む。

8.  **完了**
    1.  処理が完了した旨のメッセージ（変換レース数、出力ファイル名）をコンソールに表示してプログラムを終了する。

## 7. エラーハンドリング

-   コマンドライン引数の形式が不正な場合は、エラーメッセージを表示して終了する。
-   指定された日付の入力ファイルが存在しない場合は、エラーメッセージを表示して終了する。
-   ファイルの読み込みや処理中に予期せぬエラーが発生した場合は、エラー内容を標準エラー出力に表示して終了する。
