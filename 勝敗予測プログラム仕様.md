## 勝敗予測プログラム仕様

### 概要
このドキュメントは、勝敗予測プログラムの仕様を定義します。プログラムは、特定の条件に基づいて勝敗を予測するモデルを作成し、
残りのデータを使って勝敗結果を予測し、その結果を出力します。

### ファイル名
`racer_results_predictor.py [train|predict]`
### 実行方法
```bash
python racer_results_predictor.py
python racer_results_predictor.py train START_TRAIN_DATE END_TAIN_DATE START_TEST_DATE [END_TEST_DATE]
python racer_results_predictor.py predict
```
START_TRAIN_DATEは学習開始日、END_TRAIN_DATEは学習終了日、START_TEST_DATEは予測開始日、END_TEST_DATEは予測終了日を指定します。
END_TEST_DATEは省略可能で、省略した場合はSTART_TEST_DATEのみの日付が使用されます。
trainコマンドはモデルの学習を行い、predictコマンドは学習済みモデルを使用して予測を行います。
引数なしの場合は、学習→予測両方の処理を順に実行します。

### 入力データ
学習用の入力データとしては、`train.csv`を使用します。このCSVファイルには、各レースの参加者に関する情報が含まれています。
予測用の入力データとしては、`test.csv`を使用します。このCSVファイルには、予測対象のレースの参加者に関する情報が含まれています。

### 仕様する特徴量
以下の特徴量を使用します
- 枠番
- 選手登板
- 年齢
- 体重
- 級別
- 全国勝率
- モーター2連率
- ボート2連率

### 予測する値
- 勝敗（1: 勝ち, 0: 負け）

### 予測モデル学習
予測モデルは、ランダムフォレストを使用します。ランダムフォレストは、複数の決定木を組み合わせて予測を行うアンサンブル学習の手法です。
予測モデルは、`train.csv`のデータから、引数で指定されたSTART_TRAIN_DATEからEND_TRAIN_DATEのデータを使用して学習します。
予測モデルの評価は、`train.csv`のデータのSTART_TEST_DATEからEND_TEST_DATEの期間のデータ使用して行います。

### 予測
予測は、`test.csv`のデータを使用して行います。予測結果は、各選手ごとに勝敗を予測します。

### 出力形式
評価結果は、正解と比較し、正解率、適合率、再現率、F1スコアを計算します。出力は以下の形式でターミナルに出力を行います。

```json
{
  "accuracy": 0.85,
  "precision": 0.80,
  "recall": 0.75,
  "f1_score": 0.77
}
```

予測結果は、`predict_results.csv`というファイルに出力されます。出力ファイルの形式`test.csv`の最終列に予測結果を追加します。
