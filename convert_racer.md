# レーサー期別成績変換プログラム仕様書 (`convert_racer.md`)

## 1. 概要

本プログラムは、ボートレース公式サイトからダウンロードしたレーサー（選手）の期別成績テキストファイル（生データ）を解析し、機械学習などで扱いやすいCSV形式のデータに変換・統合することを目的とする。

## 2. 機能仕様

-   指定された年と期（前期/後期）のレーサー成績テキストファイルを読み込む。
-   テキストファイルから選手ごとの情報（基本情報、全国成績、コース別成績など）を固定長のバイト幅に基づいて抽出する。
-   抽出したデータを整形し、`data/racers.csv` という単一のCSVファイルに追記・更新する。
-   データ追加後、CSVファイルを「年」「期」「登番」の昇順でソートする。

## 3. 実行方法

### コマンドライン

```bash
python convert_racer.py <期> <年>
```

### 引数

-   `<期>`: 処理対象の期を指定する。
    -   `e`: 前期 (1月1日〜6月30日)
    -   `l`: 後期 (7月1日〜12月31日)
-   `<年>`: 処理対象の西暦年を4桁で指定する。(例: `2025`)

**実行例:**

```bash
# 2025年の前期データを処理
python convert_racer.py e 2025
```

## 4. 入力ファイル仕様

-   **パス**: `data/raw/racer/racer_{YYYY}{p}.txt`
    -   `{YYYY}`: 年 (4桁)
    -   `{p}`: 期 (`e` または `l`)
-   **フォーマット**: 固定長のテキストファイル
-   **文字コード**: UTF-8
-   **データ構造**: 1選手の情報が1行に記録されている。各データ項目はSHIFT-JISのバイト幅に基づいた固定位置に配置されている。

## 5. 出力ファイル仕様

-   **パス**: `data/racers.csv`
-   **フォーマット**: CSV (Comma-Separated Values)
-   **文字コード**: UTF-8
-   **書き込みモード**: 追記・更新
    -   ファイルが存在しない場合は新規作成し、ヘッダー行を出力する。
    -   ファイルが既に存在する場合、「年」「期」「登番」の組み合わせが重複しないデータのみを追記する。
-   **ソート順**: 処理後、ファイル全体が「年」「期」「登番」の昇順でソートされる。
-   **カラム定義**: 全140項目以上。主要な項目は以下の通り。
    -   **基本情報**: 年, 期, 登番, 名前漢字, 名前カナ, 支部, 級, 生年月日, 年齢, 身長, 体重, 血液型, 出身地
    -   **全国成績**: 勝率, 複勝率, 1着回数, 2着回数, 出走回数, 優出回数, 優勝回数, 平均ST
    -   **コース別成績 (1〜6コース)**: 進入回数, 複勝率, 平均ST, 平均ST順位, 各着順回数 (1-6着), F/L/K/S回数
    -   **その他**: 前期級, 能力指数, 養成期, 算出期間 など

## 6. 処理フロー

1.  **引数検証**: コマンドラインで受け取った「期」と「年」が有効な形式であるか検証する。
2.  **入力ファイル特定**: 引数を基に入力ファイルパスを構築し、ファイルが存在するか確認する。
3.  **データ解析 (`parse_racer_data`)**:
    -   入力ファイルを1行ずつ読み込む。
    -   各行から、SHIFT-JISのバイト幅を考慮した固定長抽出処理 (`extract_substring_by_width`) を用いて、全データ項目を抽出する。
    -   数値や日付、カナなどを適切なフォーマットに変換する。
4.  **CSV書き込み (`write_csv`)**:
    -   出力ファイル `data/racers.csv` が既に存在する場合、既存の全データを読み込み、「年」「期」「登番」の組み合わせをキーとして保持しておく。
    -   解析したデータのうち、既存データと重複しないものだけを出力ファイルに追記する。
    -   ファイルが新規作成の場合は、ヘッダー行を書き込んでから全データを書き込む。
5.  **ソート (`sort_csv_file`)**:
    -   追記・更新が完了した `data/racers.csv` を読み込む。
    -   全データ行を「年」「期」「登番」をキーとして昇順にソートし、ファイルを上書き保存する。
6.  **完了**: 処理した件数などをコンソールに出力して終了する。

## 7. エラーハンドリング

-   コマンドライン引数が不正な場合は、使用方法を表示して終了する。
-   入力ファイルが存在しない場合は、エラーメッセージを表示して終了する。
-   ファイルの読み込みやデータ解析中にエラーが発生した場合は、警告またはエラーメッセージを出力する。
