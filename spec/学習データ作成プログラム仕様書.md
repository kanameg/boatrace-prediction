## 学習データ作成プログラム仕様書

### 概要
このドキュメントは、競艇の学習データを作成するためのプログラム仕様書です。以下の内容に従って、レース結果データを読み込み、選手情報やレース情報を含むデータフレームを作成します。

### 目的
競艇のレース結果データを基に、選手やレースの特徴を抽出し、機械学習モデルの学習に使用するデータセットを作成します。

### ファイル名
`generate_tt_data.py`

※ ttは、training testの略

### プログラム言語
Python 3.10

### 実行方法
```bash
python generate_tt_data.py [train|test] START_YYYY START_MM START_DD [END_YYYY] [END_MM] [END_DD] [RACE_TRACK]
```
ここで、`START_YYYY`, `START_MM`, `START_DD`はデータの開始日、`END_YYYY`, `END_MM`, `END_DD`はデータの終了日を指定します。
`RACE_TRACK`はレース場を指定します。
trainは学習用データ、testはテスト用データを生成します。
END_YYYY, END_MM, END_DDは省略可能で、省略した場合はデフォルトで開始日と同じ日付が使用されます。
TRACE_TRACKは省略可能で、省略した場合は全てのレース場のデータが使用されます。

### 主な機能
#### レース結果データの読み込み
`racer_program.csv`を読み込みます。

#### 日付での選択
引数で指定された開始日から終了日までのレース結果を選択します。

#### ボートレース場の選択
引数で指定されたレース場のデータのみを抽出します。

#### 選手情報の抽出
選手ごとに以下の列の情報のみを抽出します。
- 枠番
- 選手登板
- 年齢
- 性別
- 体重
- 級別
- 全国勝率
- モーター2連率
- ボート2連率

### 出力ファイル
- 出力ファイル名: `{train|test}.csv`
- 出力ファイルのディレクトリ: `data/`
- 出力ファイルのエンコーディング: UTF-8
- 出力ファイルのフォーマット: CSV形式

