## ファイル情報

### ディレクトリ構造

```
/app/
├───.devcontainer/         # 開発コンテナの設定
│   ├───devcontainer.json
│   ├───docker-compose.yml
│   └───python/
│       ├───Dockerfile
│       └───requirements.txt
├───.git/                  # Gitリポジトリのメタデータ
├───.vscode/               # VSCodeエディタの設定
│   └───settings.json
├───config/                # 設定ファイル
│   └───features.json
├───data/                  # 各種データ
│   ├───raw/               # 生データ
│   │   ├───programs/
│   │   └───results/
│   ├───pred.csv
│   ├───programs.csv
│   ├───programs_header.csv
│   ├───racers.csv
│   ├───racers_header.csv
│   ├───results.csv
│   ├───results_header.csv
│   └───train.csv
├───db/                    # データベース関連のスクリプト
│   ├───import_programs.sh
│   ├───import_programs_incremental.sh
│   ├───import_racers.sh
│   ├───import_racers_incremental.sh
│   ├───import_results.sh
│   ├───import_results_incremental.sh
│   ├───programs.sql
│   ├───racers.sql
│   └───results.sql
├───man/                   # マニュアル、ドキュメント類
│   └───db更新方法.md
├───model/                 # 学習済みモデルの保存場所
├───spec/                  # 仕様書
│   ├───レーサー期別成績ダウンロードスクリプト.md
│   ├───レーサー期別成績変換プログラム仕様書.md
│   ├───レース結果データ作成プログラム仕様書.md
│   ├───学習データ作成プログラム仕様書.md
│   ├───競争成績・番組表ダウンロードスクリプト仕様書.md
│   ├───競走結果データ変換プログラム仕様書.md
│   ├───勝敗予測プログラム仕様.md
│   └───番組表データ変換プログラム仕様書.md
├───.gitignore
├───convert_program.py
├───convert_programs.sh
├───convert_racer.py
├───convert_racer.sh
├───convert_result.py
├───convert_results.sh
├───create_features.ipynb
├───create_features.py
├───download_race.sh
├───download_racer.sh
├───download_races_range.sh
├───feature.ipynb
├───lgbm_pred.ipynb
├───lgbm_pred.py
├───lgbm_train.ipynb
├───lgbm_train.py
├───README.md
├───scrape_race_results.py
├───start_jupyter.sh
├───update.sh
├───ファイル整理.md
└───特徴量方針案-01.md
```

### 主要ファイル分析

#### Pythonスクリプト（ルートディレクトリ）
- `convert_program.py` - `data/raw/programs`のテキストファイルを`data/programs.csv`に変換する。
- `convert_racer.py` - `data/raw/racer`のテキストファイルを`data/racers.csv`に変換する。
- `convert_result.py` - `data/raw/results`のテキストファイルを`data/results.csv`に変換する。
- `create_features.py` - `programs.csv`, `results.csv`, `racers.csv`を読み込み、特徴量を作成して`train.csv`と`test.csv`を出力する。
- `lgbm_pred.py` - 学習済みモデルを使い、テストデータに対して予測を行い`pred.csv`を出力する。
- `lgbm_train.py` - `train.csv`を読み込み、LightGBMモデルを学習させ`model/lightgbm_model.pkl`として保存する。
- `scrape_race_results.py` - Webからレース結果をスクレイピングする。

#### シェルスクリプト（ルートディレクトリ）
- `convert_programs.sh` - `convert_program.py`を実行し、指定された期間の番組表データを変換する。
- `convert_racer.py` - `convert_racer.py`を実行し、レーサーデータを変換する。
- `convert_results.sh` - `convert_result.py`を実行し、指定された期間のレース結果データを変換する。
- `download_race.sh` - 指定した日付のレース番組表と結果を公式サイトからダウンロードする。
- `download_racer.sh` - 指定した期のレーサー成績データを公式サイトからダウンロードする。
- `download_races_range.sh` - 指定した期間のレース番組表と結果を公式サイトからダウンロードする。
- `start_jupyter.sh` - Jupyter Labを起動する。
- `update.sh` - データダウンロード、変換、DBインポート、特徴量作成、学習、予測までの一連の処理をまとめて実行する。

#### データベース関連スクリプト（db/）
- `import_programs.sh` - `programs.csv`からSQLiteデータベースにデータをインポートする（テーブル再作成）。
- `import_programs_incremental.sh` - `programs.csv`からSQLiteデータベースに差分データをインポートする。
- `import_racers.sh` - `racers.csv`からSQLiteデータベースにデータをインポートする（テーブル再作成）。
- `import_racers_incremental.sh` - `racers.csv`からSQLiteデータベースに差分データをインポートする。
- `import_results.sh` - `results.csv`からSQLiteデータベースにデータをインポートする（テーブル再作成）。
- `import_results_incremental.sh` - `results.csv`からSQLiteデータベースに差分データをインポートする。


#### データファイル（dataディレクトリ）

**処理済みCSVファイル:**
- `programs.csv` - 変換済みの番組表データ。
- `results.csv` - 変換済みのレース結果データ。
- `racers.csv` - 変換済みのレーサーデータ。
- `train.csv` - モデル学習用の特徴量データ。
- `pred.csv` - 予測結果のデータ。

**ヘッダーファイル:**
- `programs_header.csv` - `programs.csv`のヘッダー情報。
- `results_header.csv` - `results.csv`のヘッダー情報。
- `racers_header.csv` - `racers.csv`のヘッダー情報。

**生データディレクトリ (raw):**
- `raw/programs/` - ダウンロードしたままの番組表テキストデータ。
- `raw/results/` - ダウンロードしたままのレース結果テキストデータ。

### 整理が必要な項目

#### 削除候補

1. **空のPythonファイル（6件）:**
   - convert_race_programs.py
   - extract_non_first_frame_wins.py  
   - generate_tt_data.py
   - racer_lgbm_predictor.py
   - racer_results_predictor.py
   - single_win_lgbm_predict.py

2. **重複データファイル:**
   - racer_2025e.csv（統合ファイルに含まれる）
   - racer_2025l.csv（統合ファイルに含まれる）

3. **実行権限修正が必要:**
   - download_racer_record.sh

#### 統計情報
- 総ファイル数: 5,450件
- 主要ディレクトリ数: 15件（.git除く）
- 空ファイル数: 6件
- 削除候補ファイル: 8件

### 各ディレクトリ詳細

#### /app/spec/ （仕様書ディレクトリ）
- `レーサー期別成績ダウンロードスクリプト.md`
- `レーサー期別成績変換プログラム仕様書.md`
- `レース結果データ作成プログラム仕様書.md`
- `学習データ作成プログラム仕様書.md`
- `競争成績・番組表ダウンロードスクリプト仕様書.md`
- `競走結果データ変換プログラム仕様書.md`
- `勝敗予測プログラム仕様.md`
- `番組表データ変換プログラム仕様書.md`

#### /app/model/ （機械学習モデル）
- `lightgbm_model.pkl` - LightGBMモデル
- `random_forest_model.pkl` - Random Forestモデル

#### /app/db/ （データベース関連）
- `boat_race.db` - SQLiteデータベース
- `import_programs.sh` - 番組インポート
- `import_programs_incremental.sh` - 番組増分インポート
- `import_results.sh` - 結果インポート
- `import_results_incremental.sh` - 結果増分インポート
- `programs.sql` - 番組テーブル定義
- `results.sql` - 結果テーブル定義

#### /app/config/ （設定ファイル）
- `features.json` - 特徴量設定

#### /app/data/raw/ （生データ）
- `programs/` - 番組表生データディレクトリ
- `racer/` - レーサー成績生データディレクトリ
- `results/` - レース結果生データディレクトリ
- **総計**: 1,867個のテキストファイル

### 推奨整理作業

#### 即座に削除可能
1. 空のPythonファイル（6件）
2. 重複CSVファイル（racer_2025e.csv, racer_2025l.csv）

#### ファイル権限修正
- `download_racer_record.sh` に実行権限付与

#### 検討事項
- `__pycache__/` ディレクトリ（.gitignoreで除外推奨）
- テスト・一時ファイルの整理
- ログファイルの管理

### ディレクトリ構成整理案

#### 提案するディレクトリ構造

```
/app/
├───.devcontainer/
├───.git/
├───.vscode/
├───config/                # 設定ファイル (変更なし)
├───data/                  # データファイル (変更なし)
├───db/                    # DB関連スクリプト (変更なし)
├───man/                   # ドキュメント類 (変更なし)
├───model/                 # 学習済みモデル (変更なし)
├───spec/                  # 仕様書 (変更なし)
├───notebooks/             # (新規) Jupyter Notebookを格納
│   ├───create_features.ipynb
│   ├───feature.ipynb
│   ├───lgbm_pred.ipynb
│   └───lgbm_train.ipynb
├───scripts/               # (新規) シェルスクリプトや実行のエントリーポイントを格納
│   ├───download/            # (新規) データダウンロード用スクリプト
│   │   ├───download_race.sh
│   │   ├───download_racer.sh
│   │   └───download_races_range.sh
│   ├───conversion/          # (新規) データ変換用スクリプト
│   │   ├───convert_programs.sh
│   │   ├───convert_racer.sh
│   │   └───convert_results.sh
│   └───update.sh            # 全体更新スクリプト
│   └───start_jupyter.sh     # Jupyter起動スクリプト
└───src/                   # (新規) Pythonのソースコードを格納
    ├───data_conversion/     # (新規) データ変換ロジック
    │   ├───__init__.py
    │   ├───convert_program.py
    │   ├───convert_racer.py
    │   └───convert_result.py
    ├───feature_engineering/ # (新規) 特徴量作成ロジック
    │   ├───__init__.py
    │   └───create_features.py
    ├───modeling/            # (新規) 機械学習モデルの学習・予測ロジック
    │   ├───__init__.py
    │   ├───lgbm_train.py
    │   └───lgbm_pred.py
    └───scraping/            # (新規) スクレイピングロジック
        ├───__init__.py
        └───scrape_race_results.py
```

#### 整理のポイント

- **目的**: 役割ごとにファイルを整理し、見通しを良くし、保守性を向上させる。

- **ポイント**:
    1.  **`src` ディレクトリの導入**: 
        - Pythonソースコード (`.py`) を集約。
        - `data_conversion`, `feature_engineering`, `modeling` のように役割別のサブディレクトリで管理。
    2.  **`scripts` ディレクトリの導入**:
        - シェルスクリプト (`.sh`) を集約し、Pythonコードの実行起点（エントリーポイント）として明確化。
        - `download`, `conversion` のようなサブディレクトリでさらに整理。
    3.  **`notebooks` ディレクトリの導入**:
        - Jupyter Notebook (`.ipynb`) を集約し、分析用コードと本番用コードを分離。
    4.  **ルートディレクトリのクリーンアップ**:
        - 上記の整理により、ルートディレクトリのファイル数を削減し、プロジェクト全体の構造を明確化。
